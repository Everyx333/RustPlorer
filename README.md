# RustPlorer

A fast, lightweight file manager for Windows 11, written in Rust.

Built because Explorer goes "not responding" on large or slow directories. The core design rule here is simple: **the UI thread never touches the filesystem.**

## Status

Phase 1 of 5 — foundation. See `docs/BUILD.md` to build it.
